const template = {
  head: {
    links: [
      {
        rel: 'stylesheet',
        href: 'https://cdn.jsdelivr.net/npm/@quizzy/static-pages@1.0.1/css/style.css',
      },
    ],
  },
};

const theme = {
  maxWidth: '100%',
  sidebarWidth: 280,
};

module.exports = {
  template,
  theme,
};
